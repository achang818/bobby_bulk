/** The application boundary owns today; domain evidence receives the resulting date. */
export function currentCoachingDate(now = new Date()): string {
  return `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`
}
